lancez "cmd" et naviguez vers le dossier contenant le projet

entrez "java -jar Server_app.jar" pour lancer l'appli serveur

Dans une autre cmd naviguez vers le dossier contenant le projet

entrez "java -jar Client_app.jar" pour lancer l'appli client

Pour le moment la base de données ne contient qu'une seule entrée (id=1)

t3ebt :')
